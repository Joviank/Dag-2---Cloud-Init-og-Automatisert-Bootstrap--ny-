packages:
  - git
  - docker.io
  - docker-compose

runcmd:
  - mkdir -p /opt/app
  - git clone https://github.com/Joviank/Dag-2---Cloud-Init-og-Automatisert-Bootstrap--ny-.git /opt/app
  - cd /opt/app && docker-compose up -d
